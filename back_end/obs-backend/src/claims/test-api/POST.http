@baseUrl = http://localhost:3000
@access_token = {{access_token}}   // 使用者 access token（User）

# 使用兌換碼領取優惠券（避免重複領取或兌換過期券）
POST {{baseUrl}}/claims
Content-Type: application/json
Authorization: Bearer {{access_token}}

{
  "redemptionCode": "SHOP-WELCOME-80"
}
