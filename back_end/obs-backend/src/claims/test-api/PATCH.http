@baseUrl = http://localhost:3000
@access_token = {{access_token}}   // 使用者 access token（僅能操作自己的紀錄）
@admin_token = {{admin_token}}     // 管理員 access token
@claim_id = {{claim_id}}

# 管理員或本人更新領券狀態（0 未使用 / 1 已使用 / 2 作廢）
PATCH {{baseUrl}}/claims/{{claim_id}}
Content-Type: application/json
Authorization: Bearer {{admin_token}}

{
  "state": 1
}
