@baseUrl = http://localhost:3000
@access_token = {{user_access_token}}       // 使用者 token 範例
@admin_token = {{admin_access_token}}       // 管理員 token 範例
@claim_id = {{claim_id}}

# 更新自己的領券狀態（0未使用/1已使用/2作廢）
PATCH {{baseUrl}}/claims/{{claim_id}}
Content-Type: application/json
Authorization: Bearer {{access_token}}

{
  "state": 1
}

###
# Admin 更新任意領券狀態
PATCH {{baseUrl}}/claims/{{claim_id}}
Content-Type: application/json
Authorization: Bearer {{admin_token}}

{
  "state": 2
}
