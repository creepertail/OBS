@baseUrl = http://localhost:3000
@access_token = {{access_token}}   // 使用者 access token（僅能刪自己的紀錄）
@admin_token = {{admin_token}}     // 管理員 access token
@claim_id = {{claim_id}}

# 管理員或本人刪除領券紀錄
DELETE {{baseUrl}}/claims/{{claim_id}}
Authorization: Bearer {{admin_token}}
